# 단일 EC2 + 도메인 미확보 상태를 가정한 NodePort. 도메인이 생기면 ClusterIP + k3s 기본 Traefik Ingress로 전환하라.
apiVersion: v1
kind: Service
metadata:
  name: chapchu-auth
  namespace: chapchu
spec:
  type: NodePort
  selector:
    app: chapchu-auth
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30900
